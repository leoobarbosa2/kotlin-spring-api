version: '3.8'

services:
  db:
    image: postgres:16
    container_name: forum-postgres-db
    restart: always
    environment:
      - POSTGRES_USER=USERNAME
      - POSTGRES_PASSWORD=PASSWORD
      - POSTGRES_DB=YOURDB
    ports:
      - "5432:5432"